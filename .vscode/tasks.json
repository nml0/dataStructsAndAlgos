{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake_build",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "..",
                "--config",
                "Debug"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [
                "$gcc"
            ]
        },
    ]
}
